import React, { Fragment } from "react";
import styles from "./Logo.module.css";

const Logo = () => {
  return (
    <div className={styles.logo}>
      <a href="#">
        <span>TOOCOZZY</span>
      </a>
    </div>
  );
};

export default Logo;
